services:
  openclaw-gateway:
    image: ghcr.io/openclaw/openclaw:2026.3.2
    restart: unless-stopped
    user: "0:0"
    volumes:
      - openclaw_home:/home/node
    ports:
      - "3737:3737"

volumes:
  openclaw_home:
