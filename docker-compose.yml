services:
  openclaw-gateway:
    image: ${OPENCLAW_IMAGE:-ghcr.io/openclaw/openclaw:latest}
    env_file:
      - .env
    volumes:
      - openclaw_config:/home/node/.openclaw
      - openclaw_workspace:/home/node/.openclaw/workspace
    ports:
      # IMPORTANTÍSIMO: SOLO loopback (privado)
      - "127.0.0.1:${OPENCLAW_GATEWAY_PORT:-18789}:18789"
      - "127.0.0.1:${OPENCLAW_BRIDGE_PORT:-18790}:18790"
    restart: unless-stopped
    # ✅ FIX: permitir arrancar sin configuración inicial
    command:
      ["node","dist/index.js","gateway","--bind","loopback","--port","18789","--allow-unconfigured"]

  openclaw-cli:
    image: ${OPENCLAW_IMAGE:-ghcr.io/openclaw/openclaw:latest}
    env_file:
      - .env
    network_mode: "service:openclaw-gateway"
    volumes:
      - openclaw_config:/home/node/.openclaw
      - openclaw_workspace:/home/node/.openclaw/workspace
    stdin_open: true
    tty: true
    # (Opcional) Si quieres abrir shell manualmente dentro del contenedor:
    # command: ["/bin/sh"]

volumes:
  openclaw_config:
  openclaw_workspace:
